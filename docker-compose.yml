services:
  web:
    build: .
    command: sh start.sh
    volumes:
      - ./localca_project:/app
      - static_volume:/app/staticfiles
      - ./db:/app/db
    environment:
      - TZ=UTC
      # Required: LocalCA refuses to start without a secret key.
      # Generate one with:
      #   python3 -c "import secrets; print(secrets.token_urlsafe(50))"
      # Put it in a .env file next to this compose file (see .env.example).
      - DJANGO_SECRET_KEY=${DJANGO_SECRET_KEY:?set DJANGO_SECRET_KEY in .env}
      # Hostnames this deployment is served under (comma separated).
      - DJANGO_ALLOWED_HOSTS=${DJANGO_ALLOWED_HOSTS:-127.0.0.1,localhost}
      - DJANGO_DEBUG=false
      - CSRF_TRUSTED_ORIGINS=${CSRF_TRUSTED_ORIGINS:-http://127.0.0.1,http://localhost}

  nginx:
    build:
      context: .
      dockerfile: Dockerfile.nginx
    ports:
      - "80:80"
    volumes:
      - ./localca_project:/app
      - static_volume:/app/staticfiles:ro
    depends_on:
      - web

volumes:
  static_volume:
  db: 

  #sqlite db will be in /var/lib/docker/volumes/localca_db/_data 

